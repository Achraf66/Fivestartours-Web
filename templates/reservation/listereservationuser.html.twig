{% extends 'Back/base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<center>
    <h1 class="titreadmin"> Hotels Disponibles  </h1>
</center>

<div class="example-wrapper">
    <center>
        <table border="1" class="jointure">



            {#  <th class="jointure" >Nom de l'hotel </th>
    <th class="jointure">Id de hotel  </th>
    <th class="jointure">Nom de hotel </th>
            #}



            {%for b in best%}
            {#  <tr class="jointure">


        <td class="jointure">{{b.nom}} </td>
        <td class="jointure"> {{b.id}} </td>
        <td class="jointure">{{b.nomhotel}}</td>


    </tr>#}







            <div class="card">
                <div class="container">
         <h4 class="jointure" id="left"><b> {{("Nom de hotel :")}} <centre>{{b.nomhotel}} </centre> </b></h4>
                    <p  id="left">    {{ ("Hotel :")}}       <centre>{{b.nom}}      </centre>     </p>
                    <p  id="left">    {{ ("checkin:")}} <centre>{{b.checkin|date('Y-m-d')}}  </centre>      </p>
                    <p  id="left">    {{ ("checkout:")}} <centre>{{b.checkout|date('Y-m-d')}}  </centre>      </p>
                    <p  id="left">    {{ ("endroit :")}}       <centre>{{b.endroit}}      </centre>     </p>
                    <p  id="left">    {{ ("date reservation:")}} <centre>{{b.datereservation|date('Y-m-d')}}  </centre>      </p>
                    <p  id="left">    {{ ("nomhotel:")}}    <centre>{{b.nomhotel}}  </centre>     </p>
                </div>
            </div>


        </table>

        {% endfor %}






    </center>
    {% endblock %}
